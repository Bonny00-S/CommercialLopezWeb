@{
    Layout = null;
    ViewData["Title"] = "Reset Password";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password - Commercial López</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">

    <div class="w-full max-w-md bg-white rounded-2xl shadow-xl p-8 border border-gray-200">

        <h2 class="text-3xl text-center text-orange-600 font-bold mb-6">
            Reset Password
        </h2>

        @if (ViewBag.Error != null)
        {
            <div class="bg-red-100 text-red-700 border border-red-400 px-4 py-3 rounded-lg mb-4 text-center">
                @ViewBag.Error
            </div>
        }

        @if (ViewBag.Success != null)
        {
            <div class="bg-green-100 text-green-700 border border-green-400 px-4 py-3 rounded-lg mb-4 text-center">
                @ViewBag.Success
            </div>
        }

        <form method="post" id="resetEmailForm" class="space-y-5">

            <div>
                <label class="text-gray-700 font-medium block mb-1">
                    Enter your associated email
                </label>

                <input type="email" name="email" id="email"
                       class="w-full px-4 py-2 bg-white text-gray-900 border border-gray-300 rounded-lg
                              focus:ring-2 focus:ring-orange-500 focus:outline-none"
                       placeholder="example@email.com" required />

                <span id="emailError" class="text-red-500 text-sm"></span>
            </div>

            <button type="submit"
                    class="w-full bg-orange-500 text-white py-2 rounded-lg font-semibold
                           hover:bg-orange-600 transition shadow-lg">
                Send recovery link
            </button>

        </form>
    </div>

    <script src="~/js/reset-password-validation.js"></script>

</body>
</html>
